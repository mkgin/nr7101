# Testing notes

Command line testing 

Run from this directory with:
`python3 -m nr7101.cli --verbose --verbose https://modemhostnameorip USER PASS --oid cardpage_status`

(otherwise the relative imports generate errors)